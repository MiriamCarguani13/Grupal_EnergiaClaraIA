/**
 * Bootstrap del microservicio AI standalone (opcional).
 *
 * Si AI corre embebido en EnergíaClara, este módulo NO se usa.
 * Si AI corre como microservicio separado (port 8091), aquí va
 * {@code @SpringBootApplication} + wiring + application.yml.
 *
 * Decisión arquitectónica pendiente. Ver README.md.
 */
package com.energiaclara.ai.bootstrap;
